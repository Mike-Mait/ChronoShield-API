from setuptools import setup, find_packages

setup(
    name="chronoshield",
    version="1.0.0",
    description="Python SDK for the ChronoShield API",
    packages=find_packages(),
    python_requires=">=3.10",
)
